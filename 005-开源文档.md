# 005 · 开源文档 —— 把这个项目开源，需要怎么做

> 日期：2026-08-12　·　状态：**建议稿（未执行）**
> 定位：从**开源视角**讲清楚：要开源哪些、许可证怎么定、模型/引擎等第三方资源怎么合规发布、怎么让贡献者好上手。
> 关联：`004`（技术）、`006`（部署）、`README.md`。

---

## 〇、先明确：开源的是"代码 + 页面"，模型/引擎是"第三方资源"

zitan 是公益项目（听障沟通助手）。开源收益很大：透明、可审计（"内容不离开设备"这句承诺要让别人能验证）、可协作、可被更多公益组织复用。

但**代码 ≠ 模型**。SenseVoice 模型（228MB）、onnxruntime-web、sherpa-onnx 都不是你写的，各自的许可证决定了你**能不能打包进仓库/怎么署名**。

## 一、资源许可证盘点

| 资源 | 许可证 | 能否重新分发 | 注意 |
|---|---|---|---|
| **你的代码**（`index.html`、各演示页、打包器、DSP） | **你来定**（建议 MIT 或 Apache-2.0） | ✅ | 你完全拥有 |
| **SenseVoice 模型**（`model.int8.onnx` + `tokens.txt`） | FunAudioLLM/SenseVoiceSmall，**Apache-2.0**（Alibaba） | ✅ 可，但要保留 LICENSE/署名 | 发布前请在 HF 模型卡复核；**不建议把 228MB 直接提交进 git** |
| **onnxruntime-web** | MIT | ✅ 可，需保留版权声明 | 随你的包分发没问题 |
| **sherpa-onnx**（引擎/胶水） | Apache-2.0 | ✅ 可 | 需保留 NOTICE/LICENSE |
| **Silero VAD 模型** | MIT（silero-vad） | ✅ 可 | 同上 |
| 系统 `say` 生成的 demo.wav | macOS 合成语音，**非自由许可**，仅作演示 | ⚠️ 演示用可，正式发布建议换成自由语音或让用户自录 | 可忽略或替换 |

> 一句话：**代码用你选的许可证，第三方资源各自保留它们的 LICENSE**。稳妥做法是开源仓库里只放代码 + 一份 `THIRD_PARTY_LICENSES.md`，模型等大文件放 HF/R2 下载（见 `006`），仓库 README 里给链接。

## 二、许可证选择建议

- **代码**：`MIT`（最宽松，公益项目友好）或 `Apache-2.0`（带专利条款，大厂友好）。对公益场景两者都行。
- **不用 GPL 的话**（会要求衍生作品也 GPL），除非你明确想要"传染"效果。

## 三、仓库结构建议（开源视角）

```
ZiTan/
├─ LICENSE                     # 你的代码许可证
├─ THIRD_PARTY_LICENSES.md     # 模型/引擎/Silero 各自的许可证清单 + 出处
├─ README.md                   # 项目介绍 + 快速开始 + 部署（见 006）
├─ index.html                  # 主应用（单文件）
├─ sensevoice-ort-test.html    # 语音识别演示页（路线 B）
├─ sensevoice-test.html        # sherpa 路线演示页
├─ sensevoice/                 # 大资源（.gitignore 掉，从 HF/R2 下载）
│  ├─ model.int8.onnx
│  ├─ tokens.txt
│  └─ ort/
├─ sensevoice-test/            # 工具脚本 + demo.wav
└─ docs/ 或 0xx-*.md           # 你的编号文档体系
```

关键点：**把大二进制挡在 git 外**（`.gitignore` 加 `sensevoice/`），否则仓库会爆炸（228MB+ 且每次改动重新提交）。用"下载脚本 + 校验和"（如 `scripts/download-models.sh` + `sha256sum`）让协作者自己拉模型。

## 四、开源要准备的几样东西

1. **README.md**：这是什么、给谁用、怎么跑、怎么部署、模型从哪下。→ 已规划，见 `006`。
2. **LICENSE** + **THIRD_PARTY_LICENSES.md**：代码许可 + 第三方许可清单。
3. **模型下载脚本**：`download-models.sh`（HF_ENDPOINT 兼容国内镜像）+ 校验和。
4. **隐私/安全说明**：明确"纯本地、音频不离开设备、无遥测"，这是公益项目的信任基石。
5. **行为准则 + 贡献指南**（可选但推荐）：`CODE_OF_CONDUCT.md`、`CONTRIBUTING.md`。
6. **发布渠道**：GitHub 仓库 + GitHub Pages/Cloudflare Pages 演示站点 + （可选）npm 打包 DSP 为库。

## 五、可以打包成"库"的部分（进阶）

- DSP 管线（fbank/LFR/CMVN/CTC）是**模型无关**的，可抽成独立 npm 包（如 `@zitan/sensevoice-wasm`），别人复用你的 DSP 只需接自己的模型。
- 这样 zitan 本体保持单 HTML，语音能力作为可复用库独立迭代。

## 六、发布前自检清单

- [ ] 代码里没有密钥/内网地址/个人信息
- [ ] `sensevoice/` 大文件已 .gitignore + 提供下载脚本
- [ ] THIRD_PARTY_LICENSES.md 列全第三方许可
- [ ] README 可让陌生人 5 分钟内跑起来
- [ ] 隐私声明（纯本地）写在 README 顶部
- [ ] 演示页在干净环境（无本地服务）也能加载模型（走 R2/CDN）
