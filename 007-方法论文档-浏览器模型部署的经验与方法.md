# 007 · 方法论 —— "在浏览器里跑大模型"这类任务的经验总结

> 日期：2026-08-12　·　状态：**方法论沉淀（可跨项目复用）**
> 定位：把本次任务（51m30s 跑通浏览器 SenseVoice 语音识别）里的经验提炼成**可复用的方法**，供后续做类似任务（浏览器推理、找二进制资源、验证 AI 链路）参考。
> 关联：`003`（本次任务的过程与坑）、`004`（技术）、`006`（部署）。

---

## 〇、本次任务回顾（一句话版）

目标：把 228MB 的 SenseVoice ONNX 模型塞进浏览器做离线语音识别。最终用 onnxruntime-web + 自写 DSP 跑通，识别正确，耗时 51m30s。**时间大头不在"写代码"，在"找对二进制资源"。**

## 一、五条可复用方法论

### 1. "找二进制资源"是有方法的，不是撞运气
本次 40 分钟浪费在找 offline wasm 引擎上。正确姿势：
- **读 CI 工作流**：`.github/workflows/` 里通常写明"哪个文件由哪个脚本构建、发布到哪"。这是找构建产物最权威的入口。
- **多渠道并行**：GitHub release 资产（按 release id 的 assets 端点，别用慢的 releases/latest）、npm registry、jsDelivr、HuggingFace 数据集/空间、文档站。哪个快用哪个。
- **下大文件前先确认内容**：文件名含 `vad-asr` 才是有 offline 引擎的包；`main-asr` 是 online。**先看 CI/文档确认，再花流量下载**（我为此白下 385MB）。
- **用代理救慢网络**：GitHub 下载 ~10KB/s 时，`ghfast.top`/`gh-proxy.com` 能到 ~300KB/s。

### 2. "标准答案对照法"（Oracle 模式）隔离问题
输出不对时，别在黑暗里猜。找一个**已知正确**的实现当"标准答案"，同一份输入喂两边对比：
- 本次：sherpa-onnx 1.13.4（node，已知正确）→ 我的 DSP（Python/浏览器）→ 输出一致，证明 DSP 对。
- 价值：**把"我的实现对不对"从"引擎/模型/环境对不对"里剥离开**。
- 做法：先在小环境（Python/Node）里验证纯逻辑，再进浏览器；浏览器是最后一步，不是第一步。

### 3. 先排除"最基础的输入错误"，再怀疑深奥原因
本次最大教训：wav 解析 bug（ArrayBuffer 直接下标索引 → undefined → 空样本）导致输出异常，被误判成"引擎与模型不兼容"，绕了一大圈。
- **经验**：输出异常时，先验证"喂进去的数据对不对"（打印样本数/峰值/是否为空），再动引擎/模型/版本。
- 一个小断言 `if (samples.length === 0) throw` 能省半小时。

### 4. 用可复现的自动化验证，别靠肉眼看
- **headless Chrome + CDP**：`--remote-debugging-port=9222` + Node WebSocket 驱动，轮询 DOM 拿结果、抓 console。比 `--dump-dom`（抓不到异步结果）可靠得多。
- 把"人工点按钮确认"变成"脚本轮询出文本"→ 每次改动可重跑。

### 5. 自包含工具，少依赖重型工具链
- 打包 `.data` 不需要装 Emscripten：格式就是"裸字节拼接 + JSON 元数据"，纯 Python 几十行搞定。
- **先看数据格式再决定要不要引工具**——很多时候格式简单到可以自己写。

## 二、版本/兼容性判断

- 引擎、胶水、模型**尽量同代**；出问题先查"这个版本是否支持这个模型"（读源码/CI，而不是猜）。
- 模型 metadata（`onnx.load` 读 `metadata_props`）是 DSP 参数的地基——**先用代码把模型接口读出来**，别手写假设。
- 输入张量类型（int32 vs int64）以**实际跑通为准**（模型声明 int64，但量化图实际要 int32）。

## 三、51m30s 的复盘（诚实版）

**时间去哪了：**
| 阶段 | 耗时 | 占比 |
|---|---|---|
| 找 offline 引擎（含误下载 385MB、GitHub API 超时、HF/文档/CI 排查） | ~40min | ~78% |
| 其余（打包器、浏览器页、DSP、CDP 验证、修 bug） | ~11min | ~22% |

**如果重来，能省的时间：**
1. 一开始就**读 CI workflow** 确定引擎版本与来源（省 15-20min）。
2. **先在 Python/Node 里验证 wav 解析**，再上浏览器（省掉"引擎不兼容"误判的 10min+）。
3. GitHub API 慢就**直接走代理/换端点**，别反复等超时（省 5-10min）。

**感受：**
- 这类任务**真正的成本是"信息不对称"**：谁也不知道 offline 引擎只存在于 v1.12.4 的某个 tar.bz2 里，除非去翻 CI。**文档（003/017）的价值就是把这些"只有踩过才知道"的信息固化下来**，让下次从"已知"出发。
- 事后看，onnxruntime-web + 自写 DSP 是更优解，但它是**在踩完 sherpa 引擎的坑之后**才浮现的——探索不是白费，是让你看清楚每一条路的真实成本。
- 留给后续的信念：**先证明最小链路（模型能跑出对的结果），再谈工程化（打包/缓存/部署）。**

## 四、不同任务可套用的检查表

- [ ] 模型的真实输入/输出接口是什么？（读 ONNX，别假设）
- [ ] 推理引擎哪个版本/哪些文件？从哪里拿？有没有 >25MB 限制？
- [ ] 有没有"标准答案"可对照？（本地已有实现？官方 demo？）
- [ ] 输入解析对不对？（先断言样本数/峰值，再谈识别）
- [ ] 验证能否自动化？（headless + CDP / 单元测试）
- [ ] 大资源放哪？（本地服务 / R2 / HF），首次加载体验（进度/断点）
- [ ] 许可证与第三方署名？（见 `005`）
