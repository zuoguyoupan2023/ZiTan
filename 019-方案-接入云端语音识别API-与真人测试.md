# 019 · 方案 —— 接入云端语音识别 API（第三条腿）与真人测试

> 日期：2026-08-13　·　状态：**方案（未实现：需先定厂商 + 部署 Worker）**
> 定位：给 zitan 加"第三条腿"——在浏览器原生 API（②）和本地 SenseVoice（①）之外，接入一家**云端语音识别 API**（讯飞 / 百度 / Azure 等），覆盖"无 GMS 安卓、Firefox、不想下载 228MB 模型"的设备；并给出**真人测试**方法。
> 关联：`001`（现状：原生优先 + 本地兜底）、`018`（原生 API 调研：安卓 GMS 依赖 / 地区矩阵）、`006`（部署：CF Pages + R2 + Worker）、`004`（本地 DSP）。

---

## 〇、一句话

浏览器录音 → 发到自己部署的 **Cloudflare Worker**（隐藏厂商 key）→ Worker 转发给**第三方云端识别**（讯飞/百度/Azure 等）→ 返回文本填入输入框。任何浏览器、任何地区（只要选对厂商）都能用，不依赖 Google 服务、不下载 228MB。

## 一、为什么需要第三条腿（定位）

| 场景 | ① 本地模型(SenseVoice+R2) | ② 原生 API(Web Speech) | ③ 云端 API |
|---|---|---|---|
| iOS Safari/Chrome/Edge | ⚠️ 内存紧 | ✅ 苹果服务 | ✅ |
| 安卓·有 GMS（国际版） | ✅ | ✅ 走 Google/Azure | ✅ |
| 安卓·无 GMS（华为/国行） | ✅ **唯一可靠** | ❌ 全挂 | ✅ 国内选讯飞/百度 |
| Firefox（任意） | ✅ | ❌ 不支持 | ✅ |
| 国内桌面 Chrome | ✅ | ❌ Google 被墙 | ✅ |
| 不想下 228MB 的用户 | ❌ 要下载 | ✅(能用的设备) | ✅ **轻量替代** |

→ ②③ 本质都是"云识别"，区别：② 是浏览器内置的免费云（不可控），③ 是自己接的云（**可控语言/端点/计费，还能选国内可达的厂商**）。③ 补的是② 在**无 GMS 安卓 / Firefox / 国内 Chrome** 上的空白，同时免去① 的 228MB 下载。

## 二、云厂商选择（中国 vs 国外）

| 服务 | 地区可达 | 协议 | 中文质量 | 鉴权 | 备注 |
|---|---|---|---|---|---|
| **讯飞**（语音听写） | ✅ 国内 | WebSocket 流式 | ⭐ 第一梯队 | AppID+APIKey+APISecret（HMAC） | 国内最常用，实时性好 |
| **百度**（短语音/极速版） | ✅ 国内 | REST | 好 | APIKey+SecretKey 换 token | 最简单，免费额度足 |
| **阿里云**（智能语音） | ✅ 国内 | REST/WS | 好 | AccessKey 签名 | 生态好 |
| **腾讯云**（语音识别） | ✅ 国内 | REST/WS | 好 | SecretId/SecretKey 签名 | 免费额度大 |
| **Azure Speech** | ✅ 国内用 .azure.cn，国外用全局 | REST/WS | 好 | Ocp-Apim-Subscription-Key | 文档全，国内外都可 |
| **Google Cloud STT** | ❌ 国内被墙 | REST/WS | 好 | Service Account | 国外优秀 |
| **OpenAI Whisper** | ❌ 国内被墙 | HTTP 上传 | 好 | Bearer key | 整段上传、延迟高 |

> 建议：**国内部署选讯飞（中文最好）或百度（最简单）**；要国内外统一且文档全选 **Azure**。所有厂商的 **key 都不能放前端**（会被抓走盗刷）→ 必须走后端代理。

## 三、架构：浏览器 → Worker 代理 → 云端

```
浏览器(cloud-asr-test.html / index.html)
   │  ① 录音 → 降采样 16k → PCM16 base64
   ▼  POST {audioB64}
Cloudflare Worker（/recognize）      ← 隐藏 key，唯一出口
   │  ② 换 token / 签名 → ③ 转发音频
   ▼
讯飞 / 百度 / Azure（云端识别）
   │  ④ 返回文本
   ▼
浏览器显示 / 填入输入框
```

- **为什么 Worker**：项目已用 Cloudflare（Pages/R2），一个 Worker 零成本；key 只存在 Worker 环境变量，前端永远拿不到。
- **为什么不浏览器直连**：CORS + key 泄露风险 + 计费失控。

## 四、实现

### 4.1 Worker（以百度短语音为最简可跑示例）

```js
// worker.js —— 云端识别代理。环境变量：BAIDU_API_KEY / BAIDU_SECRET_KEY
export default {
  async fetch(req, env) {
    if (req.method === 'OPTIONS') return cors(new Response('ok'));
    if (req.method !== 'POST') return cors(new Response('use POST', { status: 405 }));
    const { audioB64, rate = 16000 } = await req.json();   // audioB64 = pcm_s16le base64

    // ① 换 access_token
    const tok = await (await fetch(
      `https://aip.baidubce.com/oauth/2.0/token?grant_type=client_credentials` +
      `&client_id=${env.BAIDU_API_KEY}&client_secret=${env.BAIDU_SECRET_KEY}`)).json();

    // ② 短语音识别（pcm / 16k / 单声道 / ≤60s）
    const res = await fetch('https://vop.baidu.com/server_api', {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        format: 'pcm', rate, channel: 1, cuid: 'zitan',
        token: tok.access_token,
        speech: audioB64, len: Math.floor(audioB64.length * 3 / 4),   // 字节数
      }),
    });
    const data = await res.json();
    return cors(new Response(JSON.stringify({
      ok: data.err_no === 0, text: (data.result || []).join(''), err: data.err_msg,
    }), { headers: { 'Content-Type': 'application/json' } }));
  },
};
function cors(res) { res.headers.set('Access-Control-Allow-Origin', '*'); res.headers.set('Access-Control-Allow-Headers', 'Content-Type'); return res; }
```

> **讯飞**：WebSocket 流式，鉴权要生成 `authorization`（`api_key` + `hmac-sha256(api_secret)` 拼 base64/URL 编码），Worker 里用原生 `WebSocket` 即可，但代码比百度长一截。中文第一梯队，正式用建议讯飞。**Azure**：REST 带 `Ocp-Apim-Subscription-Key`（或用短期 token），国内外选对应端点。

### 4.2 前端采集（复用本地 ASR 的思路）

```js
async function recordAndSend(workerUrl, maxSec = 8) {
  const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
  const ctx = new AudioContext(), src = ctx.createMediaStreamSource(stream);
  const proc = ctx.createScriptProcessor(4096, 1, 1);
  const chunks = []; const t0 = ctx.currentTime;
  proc.onaudioprocess = (e) => {
    chunks.push(new Float32Array(e.inputBuffer.getChannelData(0)));
    if (ctx.currentTime - t0 >= maxSec) stop();
  };
  src.connect(proc); proc.connect(ctx.destination);
  const stop = () => { /* 停止 → 合并 → 降采样16k → 16bit → b64 → POST */ };
  ...
}
```
- 降采样、16bit 转换、base64 与本地 `sensevoice-asr.js` 同一套（`downsample` 16k + PCM16）。
- 时长限制：百度短语音 ≤60s；讯飞/流式可更长。测试页默认 8s。

### 4.3 接入 zitan 主应用的策略（可选，等 Worker 部署后再做）

- 加一个可配项 `window.ZITAN_ASR.cloudUrl`（指到 Worker）。
- 回退链变为：**原生 ② → 本地 ① → 云端 ③**：
  - 原生不可用/超时 → 本地（有 R2 模型时）
  - 本地也没得用（无 R2 / 加载失败 / 用户没等 228MB）→ 云端 ③（需要 `cloudUrl` 已配置）
- 未配置 `cloudUrl` 时行为不变（现在这样）。

## 五、真人测试

仓库附 **`cloud-asr-test.html`**（独立测试页，无需先接入主应用）：

1. **本地先跑通 Worker**：`wrangler dev worker.js`（配好环境变量 key）→ 打开 `cloud-asr-test.html`，填 `http://localhost:8787` 当 Worker URL → 点「开始录音」说话 → 停 → 看识别结果 + 延迟 + 错误日志。
2. **部署 Worker**：`wrangler deploy`（设 `BAIDU_API_KEY`/`BAIDU_SECRET_KEY` 环境变量）→ 拿到 `https://xxx.workers.dev`。
3. **真机测**：手机浏览器打开线上 `cloud-asr-test.html` → 填 Worker 线上 URL → 录音 → 看结果。**重点在国行/华为等无 GMS 设备上测**（这正是③ 补的空白）。
4. 主应用接入（§4.3）后再在 `index.html` 里复测。

### 自检清单
- [ ] Worker 环境变量已设 key，且 `curl -X POST` 直接调能返回文本（先不用前端）
- [ ] CORS 已放开（Worker 返回 `Access-Control-Allow-Origin`）
- [ ] 录音降采样到 16k / PCM16 / base64 正确（对着 3 秒静音测，应返回空或报"没识别"而非乱码）
- [ ] 中文说一句"你好，请问最近的医院在哪里？"能出字
- [ ] 国行/华为真机（无 GMS）走通这条腿
- [ ] 计费/额度确认过（各家免费额度：讯飞/百度/腾讯都有免费包）

## 六、成本 / 隐私 / 注意

- **计费**：按次/按时长，各家有免费额度（百度短语音、腾讯云、讯飞都有免费包），量小基本零成本；务必在厂商控制台设**消费上限/告警**。
- **隐私**：音频上传第三方云端 —— 需在 README/设置里写清楚"云端识别会把音频发给厂商"。隐私敏感场景走本地①。
- **key 安全**：只放 Worker 环境变量，永不进前端 / 不进 git（`.dev.vars` 本地开发用）。
- **延迟**：短音频整体上传约 1~3s；要实时字幕才上 WebSocket 流式（讯飞）。
- **失败回退**：云 API 也失败时，toast 明确错误码（如百度 `err_msg`），别静默。

## 七、决策点（下一步）

1. 选厂商：**百度（最简单，先跑通）/ 讯飞（中文最好，正式用）/ Azure（国内外统一）**
2. 部署 Worker + 真机测（本页 §五）
3. 决定是否接入主应用回退链（§4.3）—— 若 R2 已配且本地覆盖了无 GMS 设备，云端这条腿可延后
