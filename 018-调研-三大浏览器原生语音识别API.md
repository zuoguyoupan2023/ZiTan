# 018 · 调研 —— Safari / Edge / Chrome 原生语音识别 API（Web Speech API · 联网）

> 日期：2026-08-13　·　状态：**调研完成（附可实测页面）**
> 定位：调研「不依赖本地模型，直接用浏览器自带的语音识别 API（联网）」如何实现：**浏览器检测 → 调用对应 API → 语音转文字**，并给出一个可点击说话的**真人测试按钮**页面用于验证。
> 关联：`001`（zitan 现状）、`004`（本地离线 ASR 技术）、`003`（本地 ASR 踩坑）、`006`（部署）、`017`（SenseVoice 方案）。对照关系见图文：zitan 已有**离线本地** SenseVoice，本调研是**联网原生 API** 的另一条路。

---

## 〇、一句话结论

Chrome / Edge / Safari 三家**其实都实现了同一套 Web Speech API**（`SpeechRecognition` / `webkitSpeechRecognition`），并不存在三套完全不同的 API。差别只在：

| 浏览器 | 能不能用 | 谁在背后识别 | 是否必须联网 |
|---|---|---|---|
| **Chrome**（33+） | ✅ 标准 `SpeechRecognition` | 音频上传 **Google** 云端 | **必须联网**（有 Chromium 专属 on-device 新 API，见 §五） |
| **Edge**（Chromium 79+） | ✅ 同 Chrome | 音频上传 **Microsoft Azure** | **必须联网** |
| **Safari**（macOS 14.1+ / iOS 14.5+） | ⚠️ 仅 `webkitSpeechRecognition`（前缀），caniuse 标 **partial** | 苹果 `SFSpeechRecognizer`：装了**语言包**→设备端识别，否则云端 | 装了语言包可离线，否则需联网 |
| **Firefox** | ❌ 不支持（曾隐藏于 flag 后） | — | — |

> 所以"浏览器检测"真正要做的是：**探测哪个构造函数存在（`window.SpeechRecognition || window.webkitSpeechRecognition`）+ 处理各家实现差异的坑**，而不是按浏览器写三套逻辑。

---

## 一、三家支持情况（2026-08 核实）

| 项 | Chrome | Edge | Safari (macOS) | Safari (iOS) | Firefox |
|---|---|---|---|---|---|
| 构造函数 | `SpeechRecognition` 33+ | 79+（Chromium 内核） | 14.1+，仅 `webkitSpeechRecognition` | 14.5+，仅前缀 | ❌ 不支持 |
| caniuse 标注 | ✅ | ✅ | ◐ partial | ◐ partial | ❌ |
| 识别服务 | Google 云端 | Azure 云端 | 设备端（语言包）或 Apple 云端 | 同左 | — |
| 断网 | 报 `network` 错 | 报 `network` 错 | 有语言包→可离线；无语言包→失败 | 同左 | — |
| 语言包/on-device | Chrome 139+ 新增（见 §五） | Edge 150+ flag 开启 | 需在系统设置装语言包 | 同左 | — |
| 中文 | `lang='zh-CN'` ✅ | `lang='zh-CN'` ✅ | 需装简体中文语言包 | 同左 | — |

**共识**（多方来源交叉）：核心构造函数三家都可用；但 Safari 的实现最"不标准"——前缀、on-device 优先、interimResults（实时中间结果）有已知 bug（WebKit bug 288963），且部分开发者实测报告"不可用"，因此**生产代码必须做特性检测 + 兜底**。

---

## 二、浏览器检测怎么做

### 2.1 特性检测（推荐，务必优先）

不猜浏览器，直接探测 API 是否存在：

```js
const SR = window.SpeechRecognition || window.webkitSpeechRecognition;
if (!SR) {
  // Chrome/Edge/Safari 都没暴露 → 降级：提示打字，或回退到 zitan 本地 SenseVoice（见 §六）
}
```

再检测是否 Chromium 专属的 on-device 扩展（Chrome 139+ / Edge 150+）：

```js
const onDeviceApi = !!window.SpeechRecognition && typeof SpeechRecognition.available === 'function';
```

### 2.2 UA 识别（可选，用于展示/调试，不用于功能分支）

```js
function detectBrowser() {
  const ua = navigator.userAgent;
  if (/Edg\//.test(ua)) return 'Edge';
  if (/Chrome\//.test(ua) && /Safari\//.test(ua)) return 'Chrome';
  if (/Safari\//.test(ua)) return 'Safari';
  if (/Firefox\//.test(ua)) return 'Firefox';
  return 'Unknown';
}
```

> 注意：Edge 的 UA 同时含 `Chrome` 和 `Safari` 标记，**必须先用 `Edg/` 判断 Edge**，否则会被当成 Chrome。

---

## 三、调用对应 API（核心代码）

同一套代码三家通用（用前缀做了兼容）。完整要点：

```js
const SR = window.SpeechRecognition || window.webkitSpeechRecognition;
if (!SR) { /* 兜底 */ }

const rec = new SR();
rec.lang = 'zh-CN';          // BCP-47：zh-CN / zh-TW / en-US / ja-JP …
rec.continuous = false;      // true = 连续识别（一直听）；false = 说一句停
rec.interimResults = true;   // 实时中间结果（Safari 上此字段有 bug，见 §五）
rec.maxAlternatives = 1;     // 返回几个候选，取第 0 个 + confidence

rec.onstart   = () => ui('正在听…');
rec.onresult  = (e) => {
  let interim = '', final = '';
  for (let i = e.resultIndex; i < e.results.length; i++) {
    const r = e.results[i];
    const t = r[0].transcript, c = r[0].confidence;
    if (r.isFinal) final += t + ' ';
    else interim += t;
  }
  showResult(final, interim);      // 实时回显 + 最终文本 + 置信度
};
rec.onerror   = (e) => handleError(e.error);  // 见下表
rec.onend     = () => ui('已停止');

rec.start();    // 必须由用户手势触发（点击按钮里调用）
// rec.stop();  // 停止并产出最终结果
// rec.abort(); // 直接放弃本轮
```

### 错误码对照（`onerror` 的 `event.error`）

| error | 含义 | 处理建议 |
|---|---|---|
| `not-allowed` | 用户拒绝麦克风权限 | 提示去地址栏/设置开权限 |
| `no-speech` | 没检测到说话 | 提示再试 |
| `audio-capture` | 没有可用的麦克风 | 检查设备 |
| `network` | **连不上识别服务（Chrome/Edge 断网即此错）** | 提示联网，或回退本地 ASR |
| `aborted` | `abort()` 触发 | 忽略 |
| `service-not-allowed` | 浏览器/服务禁止 | 降级 |
| `language-not-supported` | 该语言不可用 | 换语言 |

### 触发前提（三家通用）

- **HTTPS 或 localhost**（安全上下文），`http://` 非 localhost 时 API 根本不可用。
- **必须用户手势**：`start()` 只能在点击/触摸回调里调用，不能页面加载自动启动。
- 首次会弹麦克风授权。

---

## 四、真人测试按钮（可实测验证页）

仓库已附独立页面 **`speech-native-test.html`**（与 `sensevoice-test.html` 风格一致，无构建）：

- 启动：项目根目录 `python3 -m http.server 8000` → 打开 `http://127.0.0.1:8000/speech-native-test.html`
- 页面提供：
  1. **浏览器/API 检测结果**：当前浏览器、`SpeechRecognition`/`webkitSpeechRecognition` 是否有、是否 on-device 扩展
  2. **语言下拉**（zh-CN / zh-TW / en-US / ja-JP / ko-KR）
  3. **真人测试按钮**：点「开始说话」→ 对着麦克风说一句 → 实时显示中间结果 → 说完停顿自动出**最终结果 + 置信度**；按钮态随 `onstart/onend` 切换
  4. **错误/状态区**：打印 `onerror` 错误码与 `onend` 事件，方便对照 §三错误表
  5. （可选）Chrome/Edge 上显示 on-device 可用性检测结果
- 验证要点：**Chrome / Edge 说话即出字（走云端，需联网）；Safari 说话出字（装了语言包走设备端）；断网测 Chrome/Edge 应报 `network` 错**。

> 用这台机器实测后，把结果（哪个浏览器能出字、出字延迟、置信度）补进本文档末尾的「实测记录」，形成可追踪记录。

---

## 五、各家的坑（按浏览器）

### Chrome
- 必须联网（Google 云端），断网 = `network` 错。
- 新 on-device API：Chrome 139+（2025 下半年）起支持 `recognition.processLocally = true` + 静态方法 `SpeechRecognition.available({langs, quality, processLocally})` / `install({langs, processLocally})`，可**完全离线**且音频不出设备。但：
  - **Android Chrome 只暴露 API、不预装模型** → `available(..., processLocally:true)` 返回 `unavailable`，一调 `start()` 就失败（2026-05 实测报告）。桌面 Chrome/ChromeOS 才带模型。
  - caniuse 标注 `available()`/`install()` 为 Chrome 151+（Edge 139+），版本号各家标注略有不一致，**用前必须调 `available()` 判断，别只看 API 存在**。
- 细节字段 `unspokenPunctuation`（省略标点）Chrome 151+ 独有。

### Edge
- 同 Chromium，但识别走 **Azure**；断网同样 `network` 错。
- 本地 on-device 需 **Edge Canary/Dev 150+** + `edge://flags`「Speech Recognition with on-device model」手动开启，正式版默认不开。
- 大部分场景与 Chrome 行为一致，`lang='zh-CN'` 可用。

### Safari（坑最多）
- 只有 `webkitSpeechRecognition`（caniuse 标 partial），务必特性检测 + 前缀。
- **on-device 优先**：装了系统语言包（设置 → Siri 与听写）则本机识别、可离线；没装语言包才可能走 Apple 云端（且不保证）。
- **interimResults 有 bug**（WebKit bug 288963）：iOS 上中间结果事件不稳定/延迟，生产建议**以 `isFinal` 结果为准**，别把 interim 当最终字。
- 需要 iOS 系统权限（麦克风 + Siri/听写），拒绝后在系统设置里开。
- 部分开发者实测在 Safari 上完全不可用 → 文档层面无法 100% 保证，**必须兜底**（打字 / 本地 ASR）。
- 别用 `navigator.mediaDevices` 那套假设——Safari 的识别不暴露给 JS 选音频源。

### 通用
- Firefox 无 API → 走兜底。
- `file://` 直开不可用（非安全上下文），必须本地起 http 或部署。

---

## 六、与 zitan 现有离线 ASR 怎么选（联网原生 vs 本地 SenseVoice）

| 维度 | 联网原生（Web Speech API） | zitan 现有（SenseVoice 本地，`001`§4） |
|---|---|---|
| 首次成本 | 0 下载，说话即用 | 懒加载 228MB 模型（约 240MB） |
| 识别质量 | 三家云端模型，中文较好 | SenseVoice-Small，中文可、带标点/语种 |
| 是否联网 | **必须联网**（Safari 装语言包例外） | **完全离线**，音频不出设备 |
| 断网 | Chrome/Edge 直接 `network` 错 | 照常工作 |
| 隐私 | 音频上云（Google/Azure/Apple） | 音频不出设备 |
| 兼容 | 除 Firefox 外可用 | 需 WASM + 内存（iOS 紧张） |
| 维护 | 零依赖、跟浏览器走 | 自管模型/DSP/推理 |

**建议组合**：
1. 联网时 → 优先用原生 API（**快、准、免下载**），一行特性检测就接入；
2. `not-allowed` / `network` / Firefox / 无 API → **回退到 zitan 本地 SenseVoice**（已有）；
3. 隐私敏感场景（医疗/个人信息）→ 强制本地。

> 这也是"浏览器检测"的最终意义：**检测到可用的原生 API 就走云端，否则回退本地**，两条路都在 zitan 里，体验无缝。

---

## 七、参考链接

- caniuse — SpeechRecognition 构造函数：https://caniuse.com/mdn-api_speechrecognition_speechrecognition
- caniuse — Speech Recognition API 总览（标 partial / UNOFF）：https://caniuse.com/speech-recognition
- MDN — SpeechRecognition：https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognition
- MDN — SpeechRecognition.available()（Chromium on-device）：https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognition/available_static
- MDN — SpeechRecognition.install()：https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognition/install_static
- Microsoft Edge Docs — Speech Recognition API：https://learn.microsoft.com/microsoft-edge/web-platform/speech-recognition-api
- WebKit bug 288963（Safari iOS interimResults 实现问题）：https://wiki.webkit.org/show_bug.cgi?id=288963
- 实测报告（Android Chrome 有 API 无模型，2026-05）：https://bagrounds.org/ai-blog/2026-05-11-1-word-meter-android-rca

---

## 附：实测记录

（用 `speech-native-test.html` 在各浏览器真机验证后填写）

| 日期 | 浏览器 | 结果 | 出字延迟 | 置信度 | 备注 |
|---|---|---|---|---|---|
| 2026-08-13 | （待测） | — | — | — | — |
