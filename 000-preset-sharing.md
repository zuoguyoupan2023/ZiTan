# 000 · 预设用语「上传 / 共享」方案讨论

> 日期：2026-08-12　·　状态：讨论中（未定稿）　·　性质：设计讨论文档

## 一、背景与目标

「字谈」的预设用语目前是**纯本地**的：存在浏览器 localStorage，用户自己编辑，只有自己用。很多听障朋友/志愿者在特定场景（医院、政务、银行、车站）会积累出很实用的短语。这些内容**只在个人手里太可惜**。

目标：
- 让用户**自愿**上传自己觉得好用的预设短语；
- 由作者（我）审核，合适的**收录并公开**；
- 其他人看到公共预设后，可以**一键添加**到自己的预设里。

## 二、现状回顾

- 预设结构：按身份分两组 `state.presets.deaf / state.presets.vol`，每组是 `{ t: 文本, k: 'fill' | 'send' }` 数组。
- 存储：localStorage（随整个 state 一起存）。
- 无任何网络功能。

## 三、方案对比

### 方案 A：纯前端 + 人工中转（零后端，成本最低）

- 客户端加「分享」：点一下把当前预设（单条或整组）导出成一段 JSON/文本。
- 用户把它粘贴到：
  - GitHub Discussion / Issue；或
  - 在线表单（Tally / 腾讯问卷 / 金数据）。
- 作者定期人工收集 → **人工审核** → 整理成一份公开 JSON（例如 `https://zitan.wewalk.world/presets.json`）。
- 客户端加「公共预设」入口：拉取该 JSON → 展示列表 → **一键导入**（按文本去重合并进本地）。

优点：零后端、零成本、隐私好、完全契合公益项目定位。
缺点：发布不实时、流程靠作者勤快、没有举报/下架入口。

### 方案 B：轻后端（Worker + KV / 数据库）

- `POST /presets` 上传：`{ role, t, k, lang, note }`。
- 后台审核队列：每条 `status = draft / approved / rejected`。
- `GET /presets?lang=zh` 公开接口：只返回 approved。
- 客户端拉取 → 一键导入。

优点：实时、可扩展、可做举报/下架、可统计。
缺点：需要维护后端、要防滥用（限流/风控）、审核成本不变。

### 方案 C：折中（**推荐起步**）

- 采集用方案 A（表单 / GitHub），发布用**静态 JSON**（作者审核后手动更新 `presets.json`）。
- 客户端只做「拉取公开 JSON → 一键导入」。
- 等有量了，再平滑升级到方案 B。

理由：成本最低、立即可用，审核质量可控；客户端接口设计成「拉一个 URL」，以后切后端不用改客户端。

## 四、数据格式（提案）

单条预设（与本地结构一致，方便复用）：

```json
{ "t": "请问最近的医院在哪里？", "k": "fill", "role": "deaf" }
```

共享包：

```json
{
  "name": "医院就诊常用语",
  "lang": "zh-CN",
  "updated": "2026-08-12",
  "items": [
    { "t": "请问最近的医院在哪里？", "k": "fill", "role": "deaf" },
    { "t": "有翻译志愿者可以帮忙吗？", "k": "send", "role": "deaf" }
  ]
}
```

导入逻辑：按 `(role + t)` 与本地去重，重复的跳过，其余 append；导入前让用户预览。

## 五、审核标准

- **内容安全**：无辱骂、敏感、违法、广告。
- **实用**：对听障沟通有实际帮助（不是吐槽/废话）。
- **无隐私**：不得包含姓名、电话、住址、证件号等个人信息（上传前 UI 必须明确提示"内容将公开"）。
- **版权**：不整段搬运他人文案。

## 六、风险与注意

| 风险 | 对策 |
|------|------|
| 恶意 / 垃圾上传 | 人工审核前置；量大后加举报 + 方案 B 的风控 |
| 隐私泄露 | 上传前强提示公开性；审核时再筛一遍 |
| 共享内容被引用后出问题 | 免责声明：公共预设仅供参考 |
| 维护成本 | 初期靠作者；可后续开放协作者 |
| 本地与公共重复 | 导入按文本去重，保留本地优先 |

## 七、待讨论问题

1. 上传入口放哪？（建议：设置 → 预设 →「分享到公共库」，点击后弹说明 + 跳表单/复制 JSON）
2. 公共预设库的入口放哪？（建议：设置 → 预设 →「公共预设库」）
3. 是否按身份（听障/志愿者）分开展示和收藏？
4. 是否需要"点赞/收藏"来给作者反馈选品？
5. 条目是否会更新？需要版本/更新时间戳吗？
6. 双语条目（zh/en）要不要一起维护？

## 八、结论 / 下一步

- **推荐先做方案 C**：表单 / GitHub 采集 + 静态 JSON 发布 + 客户端一键导入。
- 客户端改动不大（一个拉取 + 一个导入 + 两个入口），可以放进下一个迭代。
- 本方案定稿后，转成实现需求再开工。
