# 003 · 浏览器语音识别（SenseVoice）—— 尝试记录 · 最终实现 · 踩坑

> 日期：2026-08-12　·　状态：**已完成（链路跑通）**
> 定位：忠实记录本次「在 zitan 纯浏览器里跑通 SenseVoice 语音识别」做了什么尝试、最终怎么实现、中间踩了哪些坑。
> 关联：`017`（浏览器版 SenseVoice 实现方案，本次任务的方案参考）、`001`（zitan 现状）、`004`（技术文档）、`006`（部署文档）、`007`（方法论）。

---

## 一、目标

给 zitan（单 HTML 的听障沟通 PWA）加"浏览器端语音识别（ASR）"：用户打开网页 → 加载模型 → 说话 → 转成文字，**纯本地、离线、不离开设备**。

模型已就绪：`sensevoice/model.int8.onnx`（239,233,841 字节，≈228MB，sherpa-onnx 官方 SenseVoice-Small int8）+ `tokens.txt`（25055 词表）。

## 二、总体结论（先说结果）

- ✅ **两条技术路线都跑通了**，demo.wav（中文"你好，请问最近的医院在哪里？"）在浏览器里识别正确。
- 路线 A：**sherpa-onnx WASM**（017 原方案）—— 模型加载 ~2s，识别 ~90ms。
- 路线 B：**onnxruntime-web + 自写 DSP** —— 模型加载 **1.0s**，识别 **506ms**（推荐）。
- 验证手段：headless Chrome + CDP 驱动，Python onnxruntime + sherpa-onnx node 做"标准答案"对照。

## 三、尝试过程（按时间顺序）

### 3.1 尝试 0：复用 tabu-voice 的 node wasm 引擎（失败）
`tabu-voice/asr-server/node_modules/sherpa-onnx/` 里已有 `sherpa-onnx-wasm-nodejs.js/.wasm` + `sherpa-onnx-asr.js/.vad.js`，想直接拿来浏览器用。

**踩坑**：node 构建是 **NODERAWFS**（直读宿主文件系统），胶水里有一句**无条件** `require("path")`（浏览器里 `require` 不存在 → 直接 `ReferenceError`）。而且没有 MEMFS，`FS_createDataFile` 是空壳（写小文件都报 `ErrnoError 69`）。→ **node 引擎浏览器不可用，必须用浏览器版构建。**

> 小知识：Emscripten 同一份 C++ 会因 `-s NODERAWFS=1` 或 `-s FILESYSTEM=1` 编出不同行为。NODERAWFS 适合 Node 直读磁盘，浏览器要用 MEMFS + 文件注入。

### 3.2 尝试 1：模型路径直接写 URL（失败）
把 `senseVoice.model` 直接填 `http://127.0.0.1:8000/...`，期望 C++ 自己下载。

**踩坑**：sherpa-onnx 的 C++ 会 `fopen` 校验文件存在，URL 路径在校验就挂了（`does not exist`）。

### 3.3 尝试 2：找官方浏览器引擎（绕了很久，主要耗时点）

- **v1.13.5 release 的 wasm 资产全是 online（流式）引擎**（`sherpa-onnx-wasm-main-asr.*`），**没有 offline（`main-vad-asr`）引擎**。
- 误下了 `en-asr-zipformer`（167MB）和 `zh-en-asr-paraformer`（218MB），解压一看都是 `main-asr`，**白下 385MB**。
- 教训：**下大文件前先确认里面的引擎文件名**。判据：offline 引擎文件名必含 `vad-asr`（`sherpa-onnx-wasm-main-vad-asr.js/.wasm`）。
- 结论：offline 引擎只在 **v1.12.4** release 的 `sherpa-onnx-wasm-simd-1.12.4-vad-asr-*.tar.bz2` 里。下最小的 `vad-asr-en-whisper_tiny`（51.6MB）拿到引擎，模型丢弃。

> 附带发现：sherpa-onnx 官方把 offline 演示部署到了 HF Space/数据集（`jiangzhuo9357/sherpa-onnx-asr-models`，有 `.data` 模型包但没有引擎文件），GitHub release 只有 online。CI 工作流（`.github/workflows/wasm-simd-hf-space-vad-asr.yaml`）揭示了引擎版本来源——**读 CI 脚本是找构建产物最靠谱的办法**。

### 3.4 尝试 3：打包模型为 .data（成功）
浏览器版引擎用 Emscripten `.data` 预加载模型。`.data` 格式 = **各模型文件字节裸拼接** + 文件清单（filename/start/end）写进**引擎胶水 JS 的 `loadPackage({...})`**。

自己写了个纯 Python 打包器 `sensevoice-test/pack-data.py`（不依赖 emscripten）：
1. 拼接 `model.int8.onnx` + `tokens.txt` + `silero_vad.onnx` → `.data`；
2. 替换胶水里的 `PACKAGE_NAME`（改成绝对 URL）、`datafile_*`、删除旧 `FS_createPath`、替换 `loadPackage({...})` 元数据。

**踩坑 3.4a（胶水语法错误）**：替换 `loadPackage({...})` 时，`content[close_paren + 1:]` 把收尾的 `)` 也删了，多出一个 `}` → `SyntaxError: missing ) after argument list`，整个胶水不执行。改成 `content[close_paren:]` 保留 `)` 即修复。

**踩坑 3.4b（locateFile 双路径）**：胶水用 `Module.locateFile("sherpa-onnx-wasm-main-vad-asr.data", "")` 取 .data URL，我的 `locateFile = scriptDirectory + path` 会把绝对路径拼成 `/sensevoice//sensevoice/...` → 404。修正：`locateFile = (path) => '/sensevoice/' + path`（wasm 和 data 统一）。

### 3.5 尝试 4：headless Chrome 验证（关键验证手段）
`--dump-dom` 抓太快（异步初始化还没跑完），改用 **CDP（Chrome DevTools 协议）** 驱动：
- 起 `chrome --headless=new --remote-debugging-port=9222`；
- Node 脚本连 WebSocket（Node 22 自带 `WebSocket`），`Page.navigate` + `Runtime.evaluate` 轮询 DOM，等到识别结果。

### 3.6 大坑：wav 解析 bug（误导了整个调试方向）

浏览器里 `wavToFloat32(buf)` 里写了 `buf[off]`，但 `buf` 是 **ArrayBuffer（不支持下标索引，返回 undefined）** → 解析不出 `data` 块 → **喂给模型的是空样本**。

结果：sherpa 路线输出 `lang:"<|en|>"` + 空文本 → 我一度误判"**1.12.4 引擎与模型不兼容**"，去查源码版本差异、换引擎版本……白绕一大圈。

**教训：输出异常先排除最基础的"输入解析错误"，再怀疑引擎/模型兼容性。** 用已知正确的数据（node 里验证同一份 wav 解析）对照，立刻定位是解析问题。

修复：`const u8 = new Uint8Array(buf)`，用 `u8[off]` 索引。修复后**两条路线都立刻识别正确**——说明 1.12.4 引擎本来就兼容，之前的"不兼容"结论是假象。

### 3.7 尝试 5：onnxruntime-web 路线（最终推荐）

- 发现模型输入是 `x`（fbank+LFR 560 维）+ `x_length` + `language` + `text_norm`，与 npm 包 `speech-asr` 里现成的 `sense-voice-ort-worker.js` **完全同接口**。
- 先在 **Python onnxruntime** 里复现 DSP（fbank→LFR→CMVN→CTC），输出与 sherpa-onnx 1.13.4 一致（`你好，请问最近的医院在哪里？`）→ **DSP 正确性先于浏览器验证**。
- 再到浏览器：`onnxruntime-web`（tabu-voice node_modules 里有，无需下载）直接 `InferenceSession.create('/sensevoice/model.int8.onnx')`，模型加载 **1.0s**，识别 **506ms**。
- 浏览器里输入类型要 **int32**（Python ORT 也要求 int32；模型声明是 int64 但量化图实际要 int32）。

## 四、最终实现

两个可运行演示页（都在浏览器里验证通过）：

| 文件 | 路线 | 说明 |
|---|---|---|
| `sensevoice-ort-test.html` | B：onnxruntime-web | **推荐**。模型直读、加载最快、无打包 |
| `sensevoice-test.html` | A：sherpa-onnx wasm | 017 原方案，模型打包在 .data 里 |

必要资源清单见 `004`。启动方式：项目根目录 `python3 -m http.server 8000`，浏览器开 `http://127.0.0.1:8000/sensevoice-ort-test.html`。

## 五、踩坑清单（速查）

| # | 坑 | 症状 | 修复 |
|---|---|---|---|
| 1 | node wasm 引擎 NODERAWFS + 无条件 `require("path")` | 浏览器 `require is not defined` | 换浏览器版构建（v1.12.4 `vad-asr-*` 包） |
| 2 | URL 当模型路径 | C++ 校验 `does not exist` | 模型必须进 FS（.data 或 ORT 直读） |
| 3 | GitHub release 无 offline 引擎、误下 online 包 | 下错 385MB | 认准文件名含 `vad-asr`；查 CI workflow 找产物 |
| 4 | 胶水 loadPackage 替换删了 `)` | `SyntaxError` | `content[close_paren:]` 保留收尾 |
| 5 | locateFile 双路径 | .data 404 | 绝对路径统一 `/sensevoice/` 前缀 |
| 6 | **ArrayBuffer 直接下标索引** | 空样本 → 误判"引擎不兼容" | 包一层 `Uint8Array` |
| 7 | 模型输入类型 | Python ORT 报 int64→int32 | 浏览器也用 int32 |
| 8 | `--dump-dom` 抓不到异步结果 | 只能看初始状态 | 用 CDP 轮询 |
| 9 | GitHub API 超时 | release 列表拉不全 | 用 release id 的 assets 端点分页；下载走 ghfast.top 代理 |

## 六、时间花费（51m30s 大致分布）

- **找 offline 引擎 + 误下载：约 40 分钟**（最大耗时点，含 GitHub API 超时、HF 数据集/文档/CI 反复排查）。
- 其余（打包器、浏览器页、DSP、CDP 验证、修 wav bug）：约 10 分钟。
- 教训：**"找对二进制资源"往往是这类任务的最大不确定性**，应提前规划好资源获取路径（见 `007`）。
