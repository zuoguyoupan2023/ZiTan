# 004 · 浏览器语音识别（SenseVoice）—— 技术实现文档

> 日期：2026-08-12　·　状态：**可用（两条路线均验证通过）**
> 定位：从**技术视角**完整讲清「单 HTML + 语音识别」的构成：用到哪些引擎/模型/资源、浏览器端如何实现、用了什么技术、如何验证跑通。
> 关联：`003`（尝试过程与踩坑）、`017`（方案）、`005`（开源）、`006`（部署）。

---

## 〇、一句话

一个静态 HTML 页面，在浏览器里加载 **SenseVoice-Small int8 ONNX 模型（228MB）**，通过 **onnxruntime-web（WASM）** 或 **sherpa-onnx WASM** 做本地推理，把麦克风/音频转成中文文字。全程本地、离线、不离开设备。

## 一、必备资源清单

| 资源 | 大小 | 作用 | 来源 |
|---|---|---|---|
| `sensevoice/model.int8.onnx` | 228MB | SenseVoice-Small 完整 int8 模型 | `csukuangfj/sherpa-onnx-sense-voice-zh-en-ja-ko-yue-2024-07-17`（HF） |
| `sensevoice/tokens.txt` | 308KB | 25055 词表（含 `<|zh|>` `<|HAPPY|>` 等特殊 token） | 同上 |
| `sensevoice/meta.json` | 12KB | 从模型导出的 DSP 参数（LFR/CMVN/lang ID/ITN ID） | 用 `onnx` Python 从模型 metadata_props 导出 |
| `sensevoice/ort/ort.min.js` | 352KB | onnxruntime-web 主库 | npm `onnxruntime-web`（tabu-voice 依赖里现成） |
| `sensevoice/ort/ort-wasm-simd-threaded.wasm` | 12MB | onnxruntime-web 的 WASM 推理内核 | 同上（`dist/`） |

**仅 sherpa 路线需要：**
| 资源 | 大小 | 作用 |
|---|---|---|
| `sensevoice/sherpa-onnx-wasm-main-vad-asr.js/.wasm` | 93KB/11MB | sherpa-onnx offline 引擎（Emscripten） |
| `sensevoice/sherpa-onnx-asr.js` / `sherpa-onnx-vad.js` | 35KB/6KB | JS 胶水（OfflineRecognizer / VAD） |
| `sensevoice/sherpa-onnx-wasm-main-vad-asr.data` | 231MB | 模型打包数据（.data，含模型+tokens+silero_vad） |

## 二、核心技术原理

### 2.1 模型接口（关键）

读 ONNX 图（`onnx.load`）得到真实输入/输出：

```
输入:  x          float32 [N, T, 560]   ← fbank→LFR 后的特征（80维×7帧=560）
       x_length   int64   [N]           ← 实际帧数
       language   int64   [N]           ← 语言编码（auto=0, zh=3, en=4, yue=7, ja=11, ko=12, nospeech=13）
       text_norm  int64   [N]           ← 文本规范化（with_itn=14, without_itn=15）
输出:  logits     float32 [N, T', 25055]  ← CTC logits（T' 可能比输入 T 长，模型有 padding）
```

⚠️ 注意：`language`/`text_norm` 的取值是**输入编码**（元数据里的 `lang_zh=3` 等），**不是**词表里 `<|zh|>` 的 token id（那是 24884）。两者不要混。

### 2.2 DSP 管线（路线 B 自写，已对照验证）

```
PCM16 → Float32[-1,1] → buildWaveform(×32768 if normalize_samples=0, 减均值)
      → fbank: 400 样本/帧、160 位移、512 FFT、80 mel 频带、hamming 窗、preemph 0.97、取 log
      → LFR:   window=7 shift=6  → 每帧拼 7 个 80 维 → 560 维
      → CMVN:  (x + neg_mean) × inv_stddev   ← 来自 meta.json，长度 560
      → 模型:  {x, x_length, language, text_norm}
      → logits → CTC greedy 解码（跳过 blank=0、跳过 <|...|> 特殊 token）→ 文本
```

DSP 参数全部来自模型 metadata：`lfr_window_size=7, lfr_window_shift=6, normalize_samples=0, neg_mean/inv_stddev(560 维)`。

### 2.3 两条推理引擎

**路线 B：onnxruntime-web（推荐）**
- `<script src="/sensevoice/ort/ort.min.js">`，设置 `ort.env.wasm.wasmPaths = '/sensevoice/ort/'`。
- `ort.InferenceSession.create(modelUrl, { executionProviders: ['wasm'] })` —— 直接给 URL，ORT 自己 fetch 228MB。
- 输入张量：`new ort.Tensor('float32', feat, [1,T,560])`、`'int32'` 的 `x_length/language/text_norm`。
- 单线程（`numThreads=1`）避免 COOP/COEP 跨域隔离要求。
- 优势：无打包、模型加载最快（1.0s）、完全自控 DSP。

**路线 A：sherpa-onnx WASM（017 原方案）**
- 加载顺序固定：`sherpa-onnx-asr.js` → `sherpa-onnx-vad.js` → 你的代码 → `sherpa-onnx-wasm-main-vad-asr.js`（最后这个触发引擎初始化）。
- 模型经 `.data` 预加载进 Emscripten FS，`Module.setStatus` 报"Downloading data..."进度。
- `new OfflineRecognizer({ modelConfig: { tokens, senseVoice: { model: '/sense-voice.onnx', useInverseTextNormalization: 1 } } }, Module)`。
- 麦克风：`getUserMedia` → AudioContext → 降采样 16k → `CircularBuffer` → Silero VAD 切句 → 逐句 `OfflineRecognizer` 识别。
- 优势：官方维护、VAD/切句一篮子、识别极快（~90ms）。

### 2.4 .data 打包格式（路线 A 用，纯 Python 可生成）

```
sherpa-onnx-wasm-main-vad-asr.data = model.int8.onnx 字节 ++ tokens.txt ++ silero_vad.onnx（裸拼接，无头）
文件清单: 写在胶水 JS 的 loadPackage({"files":[{filename,start,end}...],"remote_package_size":N})
```
打包器：`sensevoice-test/pack-data.py`（替换 PACKAGE_NAME / datafile_ / FS_createPath / loadPackage 元数据）。

## 三、使用的技术栈

| 层 | 技术 | 说明 |
|---|---|---|
| 推理引擎 | onnxruntime-web 1.26（WASM） | 路线 B；或 sherpa-onnx WASM（路线 A） |
| 模型 | SenseVoice-Small int8（ONNX） | 非自回归 CTC，中英粤日韩 |
| 特征 | 自写 JS fbank/LFR/CMVN | 对照 sherpa 验证一致 |
| 解码 | CTC greedy + 特殊 token 过滤 | JS 实现 |
| 音频采集 | Web Audio API（getUserMedia） | 16k 单声道降采样 |
| 网页 | 原生 HTML/CSS/JS（无框架） | 贴合 zitan 单文件风格 |
| 验证 | headless Chrome + CDP、Python onnxruntime、sherpa-onnx node | 详见 §四 |

## 四、如何验证"跑通了"（三层验证）

1. **DSP 正确性（Python 先行）**：`onnxruntime` 1.22 + 自写 DSP 跑 demo.wav，输出与 `sherpa-onnx` 1.13.4 node（已知正确）完全一致 `你好，请问最近的医院在哪里？`。→ DSP 没问题，再进浏览器。
2. **浏览器端（headless Chrome + CDP）**：
   - `chrome --headless=new --remote-debugging-port=9222`
   - Node 脚本连 CDP（Node 22 内置 WebSocket），`Page.navigate` → `Runtime.evaluate` 轮询 DOM，等 `<div id="out">` 出现识别文本。
   - 顺带抓 console / exception，用于排障。
3. **人工**：浏览器打开演示页，点"自动演示"（内置 demo.wav）立刻看到文字；再试麦克风。

## 五、演示与验证数据

- `sensevoice-test/demo.wav`：系统 `say -v Tingting "你好，请问最近的医院在哪里？"` 合成，ffmpeg 转 16k/单声道/PCM16。
- 实测：路线 B 模型加载 1.0s，识别 506ms（47 帧）；路线 A 模型加载 ~2s，识别 ~90ms。

## 六、如何复现/运行

```bash
cd ZiTan
python3 -m http.server 8000          # 任意静态服务器即可（必须 HTTP，wasm/fetch 不支持 file://）
# 浏览器打开 http://127.0.0.1:8000/sensevoice-ort-test.html
```

## 七、已知限制与后续优化

- **内存**：路线 B 在桌面（Chrome）228MB 模型 + WASM 运行时无压力；iOS Safari tab 内存上限 ~1.5GB，需测试。
- **单线程**：当前 `numThreads=1`；要更快可启用多线程，但需 COOP/COEP 头（见 `006`）。
- **VAD/流式**：路线 B 目前是"3 秒一段"简单切分，可加 Silero VAD 做智能断句。
- **ITN（数字转文字）**：`text_norm=with_itn` 已启用；后续可做中文 ITN 把"一二三"还原成数字。
