# 030 · 问题移交：语音识别页「云端配置区」多轮修复无效清单

> 状态：**未解决**。本文档由 028 反馈迭代的多轮修改失败后撰写，供新对话窗口接手。
> 一句话结论：`index.html` 里针对「自填 Key 两行输入框标签」和「官方面板空行」的修改，
> 用户实测**看不到任何变化**；同批次的「隐私提醒移位」却**立即生效**。原因未知，禁止再盲改。

---

## 一、环境与验证锚点（先读这段，能省一小时）

- 项目：`ZiTan`，单文件前端 `index.html`（约 4700 行）+ Cloudflare Pages Functions（`functions/api/asr.js`）。
- 本地服务：`wrangler pages dev`，监听 `127.0.0.1:8788` 与 `[::1]:8788`（同一 workerd 进程）。
  启动时需把 `HOME`/`XDG_*` 重定向到 `.wrangler-tmp/` 内，否则 wrangler 报错。
- **服务端内容已反复用 curl 验证为最新**：`curl -s http://127.0.0.1:8788/ | grep ...`
  能命中 `fld-label`、`zitanQuotaRow" hidden`、`privacy-note` 位于 `asrCloudWrap` 之后等全部特征。
- **sw.js 不是原因**：策略是 network-first（联网必取最新）；且现行 index.html 只 unregister、不注册 SW。
- **浏览器缓存不是原因**：同一批次里 privacy-note 的移动用户立即看到了。
- **诊断锚点（已有）**：语音识别页顶部有一行小字 `UI build 028-6`。
  用户看得到它 ⇒ 拿到的是新代码。后续每次改版请手动递增此号（如 028-7）。

## 二、用户实测结论（唯一可信事实）

| 修改 | 结果 |
| --- | --- |
| 【隐私与合规提醒】移到语音识别页最底部 | ✅ **用户确认生效** |
| 自填两行输入框加动态字段标签（SecretId/SecretKey/区域…） | ❌ 用户：「依然是这两行，指代不明」 |
| 官方面板空白行消除（quota row 可见性管理） | ❌ 用户上一轮报告「空行依然存在」（该轮修复仅 JS 控制 hidden；本轮又加了 HTML 初始 `hidden`，**尚未经用户验证**） |

## 三、页面真实结构（asrPane，供定位）

```
asrPane
├─ sec-title 识别引擎
├─ #asrEngineDesc (legend)
├─ 「UI build 028-6」诊断锚点
├─ #asrEngineList            ← 引擎选项（含“云端识别：可免费用字谈官方云端…”项，engCloudDesc 词条）
├─ .vc-lang-row 识别语言 (#langSel)
└─ #asrCloudWrap             ← renderCloudConfig() 控制显隐（CLOUD_ASR_ENABLED gate）
   ├─ sec-title 云端识别
   ├─ .tabs3 #cloudSrcTabs   ← cTabZitan「字谈官方提供」/ cTabOwn「自己填写 Key」
   ├─ #zitanPane             ← #zitanModeBtns（智能切换/腾讯云/微软Azure 三按钮）
   │                           ＋ #zitanQuotaRow[hidden]（徽章 #zitanUpstreamBadges + #zitanQuotaText）
   ├─ #ownPane[hidden]       ← #ownVendorBtns（azure/tencent/openai/aliyun/volc 五按钮）
   │                           ＋ #cloudKeyRow（span.fld-label#cloudKeyLabel + input#cloudApiKey[type=password]）
   │                           ＋ #cloudExtraRow（span.fld-label#cloudExtraLabel + input#cloudExtra）
   │                           ＋ #cloudBtnBar（保存/清除）
   ├─ p.hint #cloudFreeHint
   └─ p.hint [data-i18n=cloudHint]
└─ .privacy-note【隐私与合规提醒】 ← 页面最底（已生效）
```

## 四、问题 1：自填输入框字段标签「指代不明」

### 期望
切到「自己填写 Key」后，两行输入框左侧出现随厂商变化的标签：
Azure → `API 密钥` / `区域`；腾讯云 → `SecretId` / `SecretKey`；OpenAI → `API Key` / `模型`。

### 已实施的改动（全部就位但用户说无效）
1. HTML：`#cloudKeyRow`/`#cloudExtraRow` 内各加了 `<span class="fld-label" id="cloudKeyLabel">Key</span>` 与
   `<span class="fld-label" id="cloudExtraLabel">参数</span>`（初始兜底文案）。
2. CSS（约 440 行区）：`.asr-cloud-row .fld-label { flex:0 0 auto; align-self:center; font-size:12px;
   opacity:.75; min-width:4.5em; text-align:right; user-select:none; }`
3. meta 表 `CLOUD_VENDORS`（约 2775 行）：每个厂商加 `keyLabel: () => t('fldXxx')`、`extraLabel: () => t('fldYyy')`。
4. 词条 `fldApiKey/fldRegion/fldSecretId/fldSecretKey/fldModel/fldAppKey/fldAppId`（zh/en 两语言包都有，
   分别位于 `cloudExtraPhAzure:` 行附近）。
5. `syncCloudExtraUI()` own 分支：`if (meta && meta.keyLabel) $('cloudKeyLabel').textContent = meta.keyLabel();`
   —— 该函数由 `renderCloudConfig()` 末尾调用；`renderCloudConfig()` 在打开设置语音 tab 时执行。

### 下个窗口的排查方向（按性价比排序）
1. **插桩确认执行流**：在 `syncCloudExtraUI()` own 分支首尾加 `console.log('[cloud]', v, meta && meta.keyLabel && meta.keyLabel())`，
   让用户开 DevTools Console 截图。别再猜「应该是调用了」。
2. **确认用户看的元素就是这两个 input**：让用户右键那两行 → 检查，把 Elements 面板里该节点的 outerHTML 贴出来。
   有一种可能：用户说的「两行」根本不是 `#cloudApiKey`/`#cloudExtra`（例如首次选择弹层、或其他浮层里的类似行）。
3. **t() 词条解析**：确认 `t('fldApiKey')` 在当前 uiLang 下返回中文而非 key 名（若返回 'fldApiKey' 说明词条没挂上，
   检查词条是否误放进嵌套对象/拼写不一致）。
4. **CSS 生效性**：Elements 里看 `.fld-label` 计算样式；警惕全局 `input { width:100% }` 类规则挤压兄弟元素的历史坑
   （028 曾因 `.pvbtn{width:100%}` 把按钮挤成 0 宽导致「看不见名字」）。
5. 若以上全正常而用户仍看不见 → 怀疑存在**另一套渲染路径覆盖了 asrCloudWrap 的 innerHTML**
   （全文搜索还有谁写入 `cloudKeyRow/cloudExtraRow/asrCloudWrap`）。

## 五、问题 2：官方面板的「空行」

### 现象
「智能切换 / 腾讯云 / 微软 Azure」按钮条与下方提示文本之间，有一行**看起来像没有内容的按钮**的空白带。

### 已实施的改动
- 本轮起 `#zitanQuotaRow` 带 HTML 初始 `hidden`；
- `updateQuotaRowVisibility()`：徽章与剩余次数文本皆空 → 整行 hidden；`refreshUpstreamBadges()` 成功/失败回调都会重算；
- 全局 `.hint:empty { display:none }`。

### 待办
1. 先让用户在新 build 号下复测：空行还在吗？（本轮修复尚未获得用户反馈）
2. 若仍在 → 用 DevTools Elements 从 `#zitanModeBtns` 往下逐个检查兄弟节点，找到那个真实占位的盒子。
   候选嫌疑人：`.tabs3` 的 margin、`.mode-btns` 下边距、`#cloudFreeHint` 有内容时的间距叠加、
   以及任何由 JS 注入但未赋值的节点。
3. 注意 `/api/asr/upstreams` 在本地 dev 的行为（KV 绑定、`CLOUD_ASR_ENABLED`）会影响徽章是否有内容。

## 六、已生效、不要动的部分

- 【隐私与合规提醒】位置（asrPane 最底）✅
- 朗读页试听文本体系 ✅：`PREVIEW_SAMPLES`（中/英/法/俄/西五语例句，其余英语兜底）；
  点语言组按钮 / 切换识别语言下拉 / 点「默认」均填充对应默认句；音色行 ▶ 留空时按音色语言匹配例句；
  书写系统不匹配时 toast 提醒（`detectTextScript` / `voiceExpectedScript`）。
- 语言按钮条「五个常用 + 更多/收起」，浏览组不在前五时强制显示 ✅
- 识别语言下拉从朗读页移到语音识别页顶部 ✅
- 云端 BYOK 头部透传（`X-TC-*`）、官方通道每日 30 次（025 文档）✅

## 七、给下一个窗口的工作方式要求

1. **禁止**再走「盲改 → 让用户刷新 → 问有没有好」的循环。每一步改动前先取得可观测证据
   （DevTools 截图、outerHTML、Console 日志、curl 输出四选一以上）。
2. 用户看到的现象与磁盘/服务端状态冲突时，优先怀疑**自己对渲染路径的理解错了**，而不是缓存。
3. 每次改版递增 asrPane 里的 `UI build` 号，作为「用户拿到哪一版」的唯一判据。
4. 修完一个问题就让用户验证一个问题；两个问题都关掉后才考虑收尾（提交/部署）。

## 八、变更记录

- 2025-XX-XX：创建本文档。记录 028 反馈迭代中「自填字段标签」「官方面板空行」两项修复用户实测无效的事实、
  页面结构、排查方向与工作方式约束。隐私提醒移位已确认生效并封存。
