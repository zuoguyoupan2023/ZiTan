# 020 · 需求 —— 消息增强（复制/播放键优化/收藏）、引用、粘贴、图片相册

> 日期：2026-08-13　·　状态：**需求（未实现，纯前端 UI，与 R2/云端无关）**
> 定位：给 zitan 主应用新增一批**前端交互功能**，集中在“单条消息的操作增强”和“图片/相册”两块。**本文档不涉及 R2 / Worker / 云端第③腿**（那些见 `006`、`019`）。
> 关联：`001`（当前实现总结）、`006`（部署，仅外链参考）、`019`（云端第③腿，另案 021 讨论）
> 数据载体：仍为单文件 `index.html` + 原生 JS；**新增 IndexedDB store** 存图片二进制。

---

## 〇、一句话

给每条对话气泡添加**复制、收藏、缩小播放键、引用**；输入区新增**粘贴**与**加号（发图）**；设置页新增**收藏**与**相册**两个标签页。纯前端、离线可用、不依赖任何后端。

## 一、功能总览

| # | 功能 | 入口 | 数据落点 |
|---|---|---|---|
| F1 | 复制气泡文本 | 播放键下新增“复制”按钮 | —（写剪贴板） |
| F2 | 缩小播放三角 | 现有播放键 | — |
| F3 | 收藏/取消收藏 | 气泡播放键旁新增心形按钮 | IndexedDB·收藏 store（跨会话） |
| F4 | 引用 | 长按气泡 → 输入框出现引用 → 发送后气泡内嵌引用块 | 随消息存会话 |
| F5 | 输入区粘贴 | 输入框旁粘贴按钮 | 读剪贴板进输入框 |
| F6 | 相册发图 | 输入框旁加号 → 内置相册 / 手机相册 | 消息存 IndexedDB；图片存 IndexedDB |
| F7 | 设置·收藏页 | 设置新增“收藏”标签 | 读 IndexedDB·收藏 store |
| F8 | 设置·相册页 | 设置新增“相册”标签 | 读 IndexedDB |

---

## 二、逐项需求

### F1 复制按钮（气泡内、播放键下方）

- **位置**：每条消息气泡的操作区，**播放键下方**新增一个「复制」按钮，同排播放键之后。
- **图标**：复制图标（双矩形 SVG，与现有 `ic()` 图标体系一致）。
- **行为**：点击 → 复制该条气泡的**纯文本内容**到剪贴板 → 轻 toast「已复制」（i18n）。
- **图片消息**（见 F6）：若该条是纯图片无文本，复制按钮点击复制图片（`ClipboardItem`），失败则 toast 提示无法复制图片。
- **复用**：现有播放键已经绑定 `click`，复制按钮用同理的事件委托/绑定，不破坏现有播放逻辑。

### F2 缩小播放键三角

- **现状**：播放键的三角符号**偏大**，在气泡旁显得突出。
- **需求**：把播放三角适当缩小（建议当前尺寸的 **70% 左右**，视觉只缩图标、不缩可点区域，保持易触达）。
- **实现**：调整播放键 SVG 内三角形的 `viewBox`/`width`/`height`，或给播放键加一个较小的内边距/字体大小。**不影响命中热区**（听障群体易点中优先）。
- **注意**：绿色气泡的播放键在左、蓝色在右（`.msg.vol .bubrow { flex-direction: row-reverse }`），缩放不能破坏左右布局与对齐。

### F3 收藏 / 取消收藏（心形按钮）

- **位置**：气泡操作区，播放键与复制按钮**旁边**新增一个**心形**按钮。
- **状态**：未收藏 = 空心心形（描边）；已收藏 = 实心心形（填充色高亮）。
- **行为**：点一下收藏 → 变实心 + toast「已收藏」；再点取消 → 变空心 + toast「已取消收藏」（均可 `Undo`/直接再点恢复，不强制）。
- **收藏对象**：该条消息的**引用快照**（by / text / 时间 / 若为图片消息则图片引用或对应存储 id / 所属会话 id）。**跨会话保留**（存 IndexedDB·`favorites` store，见 §三），并计入会话导出一并导出。
- **幂等**：同一消息可反复切换，不会产生重复收藏记录。

### F4 引用（长按气泡 → 输入框引用 → 气泡内嵌引用块）

- **触发**：**长按**某条气泡（约 500ms，需区分滚动/点击，避免误触）→ 弹出「引用」操作（可做轻量底部弹层或图标浮出）。
- **输入框态**：点「引用」后输入框上方/内部显示 **引用占位**：`引用 “被引消息的原文”`（微信式浅灰底 + 引用图标 + 原文摘要）。此时继续输入的是新内容。
- **发送**：发送后，该条消息的气泡内**内嵌一个引用块**（浅灰/半透明底卡片），显示：引用图标 + 被引内容摘要（文本超长省略，如截断 N 字）+ 点按可**滚动定位回原消息**（高亮闪烁一下原气泡）。
- **数据结构**：消息对象扩展为 `{ by, text, time, quote?: { id, text, by } }`（`quote.id` 指向被引消息的 id；历史旧消息无 quote 字段，兼容渲染）。
  - 被引消息若后续被**清空/删除**：引用块仍保留**文字摘要**，但定位失效时降级为「原消息不可用」，不报错。
- **微信对标**：视觉与交互参考微信“引用”样式（发送方输入态显示“引用：XXX”，发出后气泡内嵌引用卡片）。

### F5 输入区粘贴按钮

- **位置**：输入框旁边（与麦克风、加号同侧，见 F6），一个「粘贴」图标。
- **行为**：点击 → 读取系统剪贴板文本 → **自动填入输入框光标处**（追加或替换选中区间）→ toast「已粘贴」。任意位置复制的内容都能用这个按钮贴进输入。
- **权限**：现代浏览器粘贴需剪贴板读取权限，需 getUserMedia/点击手势内触发，或降级用 `document.execCommand('paste')` + 焦点提示；无权限时 toast 引导用户手动 Ctrl/Cmd+V。
- **不要抢原生粘贴**：只做按钮辅助，不拦截系统粘贴行为。

### F6 相册发图（输入框旁加号）

- **入口**：输入框旁新增**加号「＋」按钮**。
- **点击弹两个选项**（底部弹层或下拉）：
  1. **内置相册**：打开发到本地的内置图库（见 F8），多选 → 若干张 → 显示待发送图片缩略图 → 「发送」。
  2. **手机相册**：调起 `<input type="file" accept="image/*" multiple>`（移动端进入系统相册）→ 选中头像/图片 → 可多选 → 显示缩略图 → 「发送」。
- **发送**：
  - 图片以**消息形式**入 `state.messages`，渲染为**消息气泡内的图片**（保留 by 身份、走绿色/蓝色归属、可点开大字/全屏查看）。
  - 图片二进制（Blob/ArrayBuffer）存 **IndexedDB**（新增 store，不塞 localStorage，避免 5MB 爆配额）；消息内只存图片 id/引用。
- **容量与压缩**：前端对图做尺寸/质量压缩（如最长边降采样到合理值，转 webp/jpeg）控制体积；IndexedDB 无 5MB 限制但仍应控制。
- **历史兼容**：会话历史（IndexedDB `sessions`）导出的 JSON 应能带上图片（base64 或导出时附图片 blob），导出/恢复不丢图（细节可后续迭代）。

### F7 设置·收藏页

- **位置**：设置面板内新增一个标签页/区块「**收藏**」（与现有「对话历史」「语音设置」等同级）。
- **展示**：列出**所有被收藏**的消息（跨会话）：每条显示 内容摘要 + 类型（文本/图片）+ 收藏时间 + 来源会话；**点按可回到该消息所在的会话并定位**（类似现有“对话历史”点按恢复）。
- **操作**：每条右侧可**取消收藏**（✕，带 confirm）；可整体清空。
- **数据来源**：IndexedDB·`favorites` store（与 F3 同一套收藏表），结构如 `{ favorites: [ { id, text, by, time, type, imgId?, sessionId } ] }`。**可像对话历史一样导出**（见 §三 · 导出）。

### F8 设置·相册页

- **位置**：设置面板内新增标签页「**相册**」（与收藏同级）。
- **展示**：**内置相册**的图片网格（缩略图）；支持**导入**若干张图片（调起系统相册/文件选择，多选）。
- **行为**：
  - 缩略图点开大图预览；可**删除**单张；可「发送到当前会话」（跳到 F6 的待发送流程）。
  - 与 F6 内置相册**共用同一图库**（存储对象相同）。
- **数据来源**：IndexedDB 图片 store。

---

## 三、存储设计（新增）

所有新增数据（图片、收藏）统一放 **IndexedDB**，与现有会话历史（`sessions` store）同库，便于统一导出/管理。localStorage 只保留 UI 设置（`state`），不再存收藏。

### 3.1 新增 IndexedDB store：`images`（图片）

- 现有 DB：`zitan`，store `sessions`（消息文本历史）。
- 新增 store：**`images`**，key 为自增/生成 id，value 存图片二进制（`Blob`/`ArrayBuffer`）+ 元信息（`{ id, name, type, w, h, created, size, thumbnail? }`）。
- 消息引用图片：消息对象 `{ by, text?:'', imgId }`（`text` 可为空），渲染时按 `imgId` 从 `images` 取 Blob 生成 `objectURL`。
- 清理：删除消息/清空会话时，若图片 id 不再被任何消息/收藏/相册引用，则一并删除图片记录（引用计数或用后清扫）。

### 3.2 新增 IndexedDB store：`favorites`（收藏）

- key：收藏 id（生成）；value：`{ id, text, by, time, type, imgId?, sessionId, createdAt }`。
- 索引：`type`（文本/图片）、`time`（时间排序）、`sessionId`（定位回源会话）。
- 跨会话保留；取消收藏即删除该记录。
- **导出**：在「设置 → 其他 → 导出」时，除 `sessions` 外一并导出 `favorites`（含引用图片 base64/blob 引用），恢复时回填收藏 store。
- 与 `images` 互引：收藏图片消息时，`imgId` 指向 `images` store，导出需同时带出图片。

### 3.3 消息模型扩展

| 字段 | 现有 | 新增 |
|---|---|---|
| `quote` | ✗ | `{ id, text, by }`（引用块） |
| `imgId` | ✗ | 图片消息引用 |
| 收藏标记 | ✗ | 由收藏表驱动（不写回消息，避免状态耦合） |

> 兼容性：旧消息无新字段照常渲染；新字段缺失需安全兜底。

---

## 四、UI/交互细节与注意

- **操作区会变密**：气泡一行现在有 播放 + 复制 + 心形 三个按钮（蓝色气泡 row-reverse）。手机窄屏（气泡最大 88% 宽）需检查是否拥挤，必要时操作区换行或缩小间距。**大字/高对比模式下视觉不丢失**。
- **长按 vs 滚动**：F4 长按触发需处理好 `touchstart`/`touchmove` 阈值，避免与上下滚动冲突（移动端尤甚）。
- **图标体系**：新增复制/心形/加号/粘贴图标继续用现有 `ic('name', size)` SVG 内联函数，保持风格统一。
- **i18n**：所有新文案（已复制/已收藏/引用/内置相册/手机相册/粘贴/收藏页/相册页/清空收藏…）需加 `zh`+`en` 两组，走 `t()` 查表。
- **无障碍/大字**：新按钮都要有 `title`/`aria`，尺寸适配 `--fs` 缩放；不遮挡大字展示功能。
- **存储上限提示**：相册/图片达到容量上限时 toast 提示，引导用户清理。

---

## 五、验收要点

- [ ] 每条气泡可**复制**文本；图片消息可复制图片（或明确提示不支持）。
- [ ] 播放三角**明显变小**，且整体触达不变、左右布局不乱。
- [ ] 心形可收藏/取消，**跨会话**在“设置→收藏”里都能看到所有收藏。
- [ ] 长按气泡出现“引用”→ 输入框显示 `引用 “x”` → 发送后气泡内嵌引用块 → 点引用块可定位回原消息并高亮。
- [ ] 粘贴按钮能把剪贴板文本贴进输入框。
- [ ] 加号 → 内置相册 / 手机相册都能选图、多选、发送，图片作为消息气泡显示，可点开大图。
- [ ] 设置页有“收藏”和“相册”两个新标签页，相册可导入/删除/显示。
- [ ] 旧消息、旧会话、导出/恢复不因新字段崩坏；存储不爆配额。
- [ ] 中英文两组文案齐全。

---

## 六、后续 / 待定（不在本期）

- 云相册/多设备同步（需登录/后端，另议）。
- 图片消息在“大字展示”全屏轮播的扩展。
- 引用定位在会话被清空/删除后的完整降级细节。
- 云端第③腿（Worker 代理云识别）见 `019` / 待开 `021`。

---

## 七、与方案 B（R2 部署）的关系

- 本文档 **020 是纯前端 UI 功能**，与 R2 模型上传**互相独立、可并行**。
- 方案 B（R2/Worker 中转）走 `006`；云端腿走 `019`。020 不依赖它们。
- 建议分批交付：先 F1/F2/F3/F5（小尺寸、低风险），再 F4（引用，中等），最后 F6/F7/F8（图片相册，较大），每批 UI 可见后确认。
